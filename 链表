#include <iostream>
using namespace std;

typedef struct ListNode{
    int data;
    ListNode* next;
}node;

node* create_node(int b)
{
	node *head = new node;
 	node *pre = head; //保存上一个节点
 	
 	for(int i=0; i<5; i++)
 	{
 		node *num = new node;
 		cin>>num->data;
 		pre->next = num; //上一个节点指向当前
 		pre = num; //在生成下一个新的节点之前，将当前节点赋给上一个节点
	}
	return head->next;
}

void output(int a)
{
	node *tmp = new node;
	tmp = create_node(a);
	for(int j=0; j<5; j++)
	{
		cout<<tmp->data<<endl;
		tmp = tmp->next;
	}
	
}

int main(){
    
	int m;
	for(int k=0; k<5; k++)
	{
		output(m);
	}
	
    return 0;
}
